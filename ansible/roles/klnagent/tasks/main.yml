---
# Роль не реализована. Задачи перечислены в README.md.
# Пустой список задач безопаснее частичной реализации: частично
# работающая роль на узле в аттестованном контуре оставляет систему
# в неопределённом состоянии.
- name: Роль не реализована — используйте hosts/linux-debian/10_install-netagent.sh
  ansible.builtin.fail:
    msg: >-
      Роль klnagent является заготовкой. Средства управления конфигурацией
      в текущем развёртывании не применяются. Установка выполняется
      сценарием hosts/linux-debian/10_install-netagent.sh.
