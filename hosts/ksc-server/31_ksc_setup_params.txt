# ============================================================================
#  Параметры тихой установки Сервера администрирования Kaspersky Security Center
#
#  ВНИМАНИЕ. Набор и написание ключей отличаются между версиями KSC (14.2, 15.x)
#  и между дистрибутивами (полный пакет / отдельный MSI). Перед применением
#  ОБЯЗАТЕЛЬНО сверьте содержимое этого файла с разделом документации
#  "Установка Сервера администрирования в тихом режиме" для вашей версии
#  и при необходимости приведите ключи в соответствие.
#
#  Строки, начинающиеся с '#', игнорируются скриптом 30_Install-KscServer.ps1.
#  Остальные строки склеиваются через пробел и передаются setup.exe как есть.
#
#  Проверить фактически поддерживаемые ключи можно так:
#      setup.exe /?
#      msiexec /i "<путь>\Kaspersky Security Center.msi" /L*v install.log
#  (второй вариант — установка с полным журналом, где видны все свойства).
#
#  Рекомендуемый порядок: первый раз ставить интерактивно (без -Silent),
#  зафиксировать выбранные значения, затем перенести их сюда для тиражирования.
# ============================================================================

# --- Базовые ключи запуска ---------------------------------------------------
/s

# --- Свойства установки (передаются через /v"...") ---------------------------
# Раскомментируйте и приведите к синтаксису вашей версии.
#
# /v"EULA=1 PRIVACYPOLICY=1 \
#    INSTALLDIR=\"C:\Program Files (x86)\Kaspersky Lab\Kaspersky Security Center\" \
#    SHAREDDIR=\"E:\KLSHARE\" \
#    NETSIZE=3 \
#    SERVERADDRESS=ksc.domain.local \
#    SERVERPORT=13000 \
#    SERVERSSLPORT=13291 \
#    DBTYPE=MySQL \
#    DBSERVERNAME=localhost \
#    DBPORT=3306 \
#    DBNAME=ksc \
#    DBUSER=kscadmin \
#    DBPASSWORD=<пароль> \
#    SERVICEACCOUNT=DOM\svc_ksc \
#    SERVICEPASSWORD=<пароль> \
#    /qn /L*v C:\ProgramData\KscDeployment\logs\ksc-install.log"

# --- Замечания по безопасности ----------------------------------------------
# 1. НЕ храните в этом файле реальные пароли. Для тиражирования используйте
#    подстановку значений из парольного хранилища на этапе запуска.
# 2. Файл параметров содержит сведения о конфигурации АС и относится к ДСП:
#    хранение и передача — только внутри аттестованного контура.
